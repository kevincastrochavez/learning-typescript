let age: number;
age = 12;

let userName: string;
userName = 'Kevin';

let isStudent: boolean;
isStudent = true;

// Types in arrays
let hobbies: string[];
hobbies = ['Sports', 'Cooking'];

// Define the type of an object
let person: {
  name: string;
  age: number;
};

person = {
  name: 'Kevin',
  age: 25,
};

// Array of objects
let people: {
  name: string;
  age: number;
}[];
